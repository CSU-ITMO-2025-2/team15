apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-app
spec:
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: main-app
  template:
    metadata:
      labels:
        app: main-app
    spec:
      containers:
        - name: app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          resources:
{{ toYaml .Values.resources.main | indent 12 }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: [ "python", "app.py" ]
          env:
            - name: CONNECTION_URI
              valueFrom:
                secretKeyRef:
                  name: database-pass
                  key: CONNECTION_URL
            - name: RABBIT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-pass
                  key: RABBITMQ_DEFAULT_PASS
            - name: RABBIT_USER
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-pass
                  key: RABBITMQ_DEFAULT_USER
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: jwt-pass
                  key: SECRET_KEY

          envFrom:
            - configMapRef:
                name: {{ .Release.Name }}-config
          # --- PROBES ---
          startupProbe:
            httpGet:
              path: /health
              port: 8081
            failureThreshold: 10
            periodSeconds: 3
          # 2. Проверка, что приложение не зависло
          livenessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
          # 3. Проверка, готово ли принимать трафик
          readinessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
